{
  "entities": {
    "PublicIngredients": {
        "title": "Public Ingredients",
        "description": "A list of ingredients saved publicly by any user.",
        "type": "object",
        "properties": {
            "ingredients": {
                "type": "array",
                "items": {
                    "type": "string"
                },
                "description": "The list of ingredients."
            },
            "createdAt": {
                "type": "string",
                "format": "date-time",
                "description": "The timestamp when the ingredients were saved."
            },
            "isPublic": {
                "type": "boolean",
                "description": "Flag to indicate if the entry is public."
            }
        },
        "required": ["ingredients", "createdAt", "isPublic"]
    }
  },
  "auth": {
    "providers": []
  },
  "firestore": {
    "/publicIngredients/{ingredientId}": {
        "schema": {
            "$ref": "#/entities/PublicIngredients"
        },
        "description": "Stores lists of ingredients saved publicly by users."
    }
  }
}
